# Cashflow

A polished Cash App-inspired finance dashboard prototype built with Next.js, TypeScript, and CSS. This project is a UI prototype only and does not process real money or connect to financial accounts.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.
